# วิธีใช้เว็บ เจ๊สุมาลีรับจ้างแซ็กรังติดกระดุม

## 1. แก้ข้อมูลร้าน
เปิดไฟล์ `js/config.js` ด้วย Notepad แล้วแก้ข้อความในเครื่องหมาย "..." ได้เลย
(ชื่อร้าน, เบอร์โทร, LINE, ที่อยู่, เวลาทำการ, ลิงก์ Google Map)

> ชื่อร้านที่ขึ้นตอนแชร์ลิงก์ใน LINE/Facebook อยู่ใน `index.html` บรรทัดบน ๆ (`<title>` และ `og:title`) — ถ้าเปลี่ยนชื่อร้าน ให้แก้ตรงนั้นด้วย

## 2. ใส่รูปผลงาน
1. ก๊อปรูปไปไว้ในโฟลเดอร์ `images/works/`
2. เพิ่มชื่อไฟล์ใน `config.js` ส่วน `works` เช่น
   `{ file: "รูปใหม่.jpg", category: "รังดุม", caption: "รังดุมเสื้อเชิ้ต" },`
- ย่อรูปให้กว้างประมาณ 1200–1600 px ก่อนใส่ เว็บจะเปิดเร็วขึ้นบนมือถือ
- รูปหน้าปก (พื้นหลังหน้าแรก + รูปตอนแชร์ลิงก์): ตั้งชื่อ `cover.jpg` ใส่ไว้ใน `images/`

## 3. ใส่คลิป
- **แนะนำ:** อัปขึ้น YouTube (ตั้งเป็น "ไม่เป็นสาธารณะ/Unlisted" ก็ได้) แล้ววางลิงก์ใน `youtube: "..."`
- หรือใส่ไฟล์ `.mp4` ในโฟลเดอร์ `videos/` แล้วใส่ชื่อไฟล์ใน `file: "..."` (ไฟล์ควรเล็กกว่า ~20 MB ต่อคลิป)

## 4. เว็บออนไลน์
- ลิงก์เว็บ: https://dechaphoprre-hub.github.io/jsu-factory/
- ไฟล์เว็บเก็บอยู่ที่ GitHub: https://github.com/dechaphoprre-hub/jsu-factory
- แก้ไฟล์ในเครื่องแล้ว ให้สั่ง `git add -A`, `git commit -m "แก้ไข"`, `git push` (หรือบอก Claude ให้ทำให้) เว็บจะอัปเดตภายใน 1–2 นาที

## 5. ใส่ลิงก์ใน Google Map
1. เข้า Google Business Profile (ค้นชื่อร้านใน Google ตอนล็อกอินบัญชีเจ้าของ) → **แก้ไขโปรไฟล์**
2. ช่อง **เว็บไซต์** → วางลิงก์จากข้อ 4 → บันทึก
